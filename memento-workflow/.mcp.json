{
    "mcpServers": {
        "memento-workflow": {
            "command": "uv",
            "args": [
                "run",
                "--project", "${CLAUDE_PLUGIN_ROOT}",
                "python", "-m", "scripts.cli"
            ],
            "cwd": "${CLAUDE_PLUGIN_ROOT}"
        }
    }
}
