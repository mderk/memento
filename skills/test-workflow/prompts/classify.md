# Classify Items

Classify each item by character count: "short" (<=4 chars) or "long" (>4 chars).

Items: {{results.detect.structured_output.items}}

Return a plain text summary like: "alpha=long, beta=short, gamma=long"
